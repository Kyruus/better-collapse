// Generated by CoffeeScript 1.4.0

jQuery(function() {
  var Accordion;
  Accordion = (function() {

    function Accordion(element, options) {
      var href;
      this.$element = $(element);
      this.$collapse = $(this.$element.attr("data-target") || (href = this.$element.attr("href")) && href.replace(/.*(?=#[^\s]+$)/, ""));
      this.settings = $.extend({}, this.defaults, options);
      this.open_element = this.$element.find(this.settings.openTarget);
      this.closed_element = this.$element.find(this.settings.closeTarget);
      this.render();
    }

    Accordion.prototype.render = function() {
      if (this.is_open()) {
        return this.show();
      } else {
        return this.hide();
      }
    };

    Accordion.prototype.show = function() {
      this.open_element.show();
      return this.closed_element.hide();
    };

    Accordion.prototype.hide = function() {
      this.closed_element.show();
      return this.open_element.hide();
    };

    Accordion.prototype.is_open = function() {
      if (this.$collapse.parent().find(".collapse.in").filter(":visible").length > 0) {
        return true;
      }
      return false;
    };

    return Accordion;

  })();
  Accordion.prototype.defaults = {
    openTarget: '.js-open-target',
    closeTarget: '.js-close-target'
  };
  return $.fn.accordion = function(options) {
    return this.each(function() {
      var $this, plugin, settings;
      $this = $(this);
      plugin = $this.data('accordion');
      if (plugin === void 0) {
        settings = $.extend({}, $this.data(), options);
        plugin = new Accordion(this, settings);
        $this.data('accordion', plugin);
      }
      if ($.type(options) === 'string') {
        return plugin[options]();
      }
    });
  };
});

$(document).on("click.collapse.data-api", "[data-toggle=collapse]", function(e) {
  var $this;
  $this = $(this);
  return $this.accordion('render');
}).ready(function() {
  return $("[data-toggle=collapse]").accordion();
});
